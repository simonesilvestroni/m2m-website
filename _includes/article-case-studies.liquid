<li>
	<h4>{{ post.title }}</h4>
	<img src="{{ site.url }}/assets/images/{{ post.featimage-name }}" alt="{{ post.featimage-alt }}">
	<div>
		{%- for tag in post.tags limit:3 %}
		<span>{{ tag }}</span>
		{%- endfor %}
	</div>
	<a href="{{ post.url }}">See project</a>
</li>